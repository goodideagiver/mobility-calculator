import styled from 'styled-components';

const NoItemsMessage = styled.div`
	height: 100%;
	width: 100%;
	display: grid;
	place-items: center;
	font-size: 3rem;
	opacity: 0.3;
`;

export default NoItemsMessage;
